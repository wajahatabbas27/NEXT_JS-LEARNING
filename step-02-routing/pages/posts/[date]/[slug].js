import React from "react";

const [slug] = () => {
  return (
    <div>
      It is the dynamic path for every data that is coming all the way with any
      name will be routed over here.
    </div>
  );
};

export default [slug];
